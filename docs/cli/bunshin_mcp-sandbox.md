## bunshin mcp-sandbox

MCP tool discovery + sandboxed code execution demo

### Synopsis

Demonstrates MCP tool discovery and sandboxed code execution:

  1. Creates a fake MCP client pre-loaded with a "run_python" tool.
  2. Routes tool calls through a MockBackend sandbox.
  3. Injects an API credential per-request via context (credentials package).

In production, replace FakeMCPClient with a real MCPClient pointing at an
MCP server, and replace MockBackend with E2BBackend or DockerBackend.

Environment variables:
  BUNSHIN_API_KEY      API key injected as the MCP server credential (default: demo-key-123)
  BUNSHIN_SERVER_URL   MCP server URL

```
bunshin mcp-sandbox [flags]
```

### Examples

```
  bunshin mcp-sandbox
  bunshin mcp-sandbox --server-url http://mcp.example.com
  BUNSHIN_API_KEY=my-real-key bunshin mcp-sandbox
```

### Options

```
  -h, --help                help for mcp-sandbox
      --server-url string   MCP server URL (default "http://localhost:8811")
```

### Options inherited from parent commands

```
      --api-key string     API key for the chosen provider
      --log-level string   Log level: debug|info|warn|error (default "info")
      --model string       Model ID (default: provider-specific)
      --provider string    LLM provider: fake|openai|anthropic|google|ollama (default "fake")
```

### SEE ALSO

* [bunshin](bunshin.md)	 - bunshin-go — Go LangChain/LangGraph/LangSmith clone

###### Auto generated by spf13/cobra on 3-Jun-2026
