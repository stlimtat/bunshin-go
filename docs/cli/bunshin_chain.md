## bunshin chain

Two-step entity extraction chain demo

### Synopsis

Demonstrates a two-step sequential chain:

  Step 1 (fast model):      Extract key entities (words longer than 5 chars).
  Step 2 (reasoning model): Summarise entity count and list.

Provider and model are selected from --provider / BUNSHIN_PROVIDER.

```
bunshin chain [flags]
```

### Examples

```
  bunshin chain --input "Apple and Google are big tech companies"
  BUNSHIN_PROVIDER=openai BUNSHIN_API_KEY=sk-... bunshin chain \
    --input "Anthropic and OpenAI are AI labs"
```

### Options

```
  -h, --help           help for chain
      --input string   Text to process (default "The quick brown fox jumps over the lazy dog near the riverside")
```

### Options inherited from parent commands

```
      --api-key string     API key for the chosen provider
      --log-level string   Log level: debug|info|warn|error (default "info")
      --model string       Model ID (default: provider-specific)
      --provider string    LLM provider: fake|openai|anthropic|google|ollama (default "fake")
```

### SEE ALSO

* [bunshin](bunshin.md)	 - bunshin-go — Go LangChain/LangGraph/LangSmith clone

###### Auto generated by spf13/cobra on 3-Jun-2026
