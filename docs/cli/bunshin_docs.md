## bunshin docs

Generate CLI documentation in markdown

### Synopsis

Generates one markdown file per subcommand under --output-dir.

The generated files are structured for LLM consumption: each file contains
the command's synopsis, description, flag reference, and usage examples.
Feed the output directory to your RAG pipeline or paste into a context window.

Files produced:
  bunshin.md
  bunshin_serve.md
  bunshin_health.md
  bunshin_llm.md
  bunshin_chain.md
  bunshin_agent.md
  bunshin_mcp-sandbox.md
  ... (one per subcommand)

```
bunshin docs [flags]
```

### Examples

```
  bunshin docs
  bunshin docs --output-dir ./docs/cli
```

### Options

```
  -h, --help                help for docs
      --output-dir string   Directory to write markdown files into (default "./docs/cli")
```

### Options inherited from parent commands

```
      --api-key string     API key for the chosen provider
      --log-level string   Log level: debug|info|warn|error (default "info")
      --model string       Model ID (default: provider-specific)
      --provider string    LLM provider: fake|openai|anthropic|google|ollama (default "fake")
```

### SEE ALSO

* [bunshin](bunshin.md)	 - bunshin-go — Go LangChain/LangGraph/LangSmith clone

###### Auto generated by spf13/cobra on 3-Jun-2026
