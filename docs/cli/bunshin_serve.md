## bunshin serve

Start the HTTP workflow server

### Synopsis

Start the bunshin-go HTTP workflow server.

Exposes:
  POST /workflows/{id}        synchronous workflow execution
  GET  /workflows/{id}/stream SSE streaming execution
  GET  /health                healthcheck (Docker/LB probe)
  GET  /live                  liveness probe (Kubernetes)
  GET  /ready                 readiness probe (Kubernetes)
  GET  /debug/pprof/*         Go pprof profiling

Environment variables (BUNSHIN_ prefix):
  BUNSHIN_ADDR        Listen address (default: :8080)
  BUNSHIN_LOG_LEVEL   Log level: debug|info|warn|error

```
bunshin serve [flags]
```

### Examples

```
  bunshin serve
  bunshin serve --addr :9090
  BUNSHIN_ADDR=:9090 bunshin serve
```

### Options

```
      --addr string   HTTP listen address (default ":8080")
  -h, --help          help for serve
```

### Options inherited from parent commands

```
      --api-key string     API key for the chosen provider
      --log-level string   Log level: debug|info|warn|error (default "info")
      --model string       Model ID (default: provider-specific)
      --provider string    LLM provider: fake|openai|anthropic|google|ollama (default "fake")
```

### SEE ALSO

* [bunshin](bunshin.md)	 - bunshin-go — Go LangChain/LangGraph/LangSmith clone

###### Auto generated by spf13/cobra on 3-Jun-2026
