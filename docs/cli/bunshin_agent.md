## bunshin agent

Agent loop with arithmetic tools demo

### Synopsis

Demonstrates a full agent loop using bunshin-go's graph package.

The agent has two built-in tools:
  calc   Evaluate arithmetic expressions: "2 + 2", "6 * 7"
  echo   Echo the input back

The agent loops (reason → tool → reason) until it produces a final answer.
Natural-language questions like "What is 6*7?" are handled by extracting
the arithmetic expression before dispatching to the calc tool.

```
bunshin agent [flags]
```

### Examples

```
  bunshin agent --question "3 + 4"
  bunshin agent --question "What is 6*7?"
  bunshin agent --question "Tell me something"
```

### Options

```
  -h, --help              help for agent
      --question string   Question for the agent to answer (default "3 + 4")
```

### Options inherited from parent commands

```
      --api-key string     API key for the chosen provider
      --log-level string   Log level: debug|info|warn|error (default "info")
      --model string       Model ID (default: provider-specific)
      --provider string    LLM provider: fake|openai|anthropic|google|ollama (default "fake")
```

### SEE ALSO

* [bunshin](bunshin.md)	 - bunshin-go — Go LangChain/LangGraph/LangSmith clone

###### Auto generated by spf13/cobra on 3-Jun-2026
