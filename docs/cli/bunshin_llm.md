## bunshin llm

Send a single message to an LLM provider

### Synopsis

Demonstrates the simplest possible bunshin-go usage: one LLM call.

Selects the provider from --provider (or BUNSHIN_PROVIDER).
With --provider fake (default) no API key is needed and the response is canned.
Set --provider openai --api-key $OPENAI_API_KEY for a real call.

```
bunshin llm [flags]
```

### Examples

```
  # Fake provider (no key needed)
  bunshin llm --message "What is Go?"

  # OpenAI (once adapter is implemented)
  bunshin llm --provider openai --api-key $OPENAI_API_KEY --model gpt-4o-mini \
    --message "What is Go?"

  # Via environment variables
  BUNSHIN_PROVIDER=openai BUNSHIN_API_KEY=sk-... bunshin llm --message "What is Go?"
```

### Options

```
  -h, --help             help for llm
      --message string   User message to send to the LLM (default "What is Go?")
```

### Options inherited from parent commands

```
      --api-key string     API key for the chosen provider
      --log-level string   Log level: debug|info|warn|error (default "info")
      --model string       Model ID (default: provider-specific)
      --provider string    LLM provider: fake|openai|anthropic|google|ollama (default "fake")
```

### SEE ALSO

* [bunshin](bunshin.md)	 - bunshin-go — Go LangChain/LangGraph/LangSmith clone

###### Auto generated by spf13/cobra on 3-Jun-2026
