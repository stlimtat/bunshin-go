## bunshin health

Check server health (exits 0 if healthy, 1 otherwise)

### Synopsis

Sends a GET /health request to the running bunshin server and exits 0 on success.

Designed to be used as a Docker HEALTHCHECK command inside the container image
(no curl/wget needed — the binary health-checks itself).

Environment variables:
  BUNSHIN_HEALTH_ADDR   Server base URL (default: http://localhost:8080)

```
bunshin health [flags]
```

### Examples

```
  bunshin health
  bunshin health --addr http://localhost:9090
  BUNSHIN_HEALTH_ADDR=http://remotehost:8080 bunshin health
```

### Options

```
      --addr string   Server base URL (default "http://localhost:8080")
  -h, --help          help for health
```

### Options inherited from parent commands

```
      --api-key string     API key for the chosen provider
      --log-level string   Log level: debug|info|warn|error (default "info")
      --model string       Model ID (default: provider-specific)
      --provider string    LLM provider: fake|openai|anthropic|google|ollama (default "fake")
```

### SEE ALSO

* [bunshin](bunshin.md)	 - bunshin-go — Go LangChain/LangGraph/LangSmith clone

###### Auto generated by spf13/cobra on 3-Jun-2026
